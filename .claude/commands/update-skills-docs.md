Read every file in .claude/commands/ — these are the project skills. For each one, extract: the skill name (the filename without .md), a one-sentence description of what it does, when to invoke it, and any arguments it accepts.

Rewrite docs/skills.md as a simple reference page with:
- A brief intro sentence
- A summary table: skill | arguments | when to use
- One short section per skill (3–5 lines max) with the one-liner, the argument if any, and a concrete example invocation

Do not copy the full skill content — summarize only. The goal is a quick-reference page, not a duplicate of the skill files.

Do not modify any other file.
